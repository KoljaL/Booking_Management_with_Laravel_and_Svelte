meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/login
  body: formUrlEncoded
  auth: none
}

auth:basic {
  username: 
  password: 
}

auth:bearer {
  token: 
}

body:form-urlencoded {
  email: admin@example.com
  password: password
}

vars:post-response {
  admtoken: res.body.token
}

tests {
  // console.log(bru);
  // console.log(bru.collectionVariables.adm_token);
}
